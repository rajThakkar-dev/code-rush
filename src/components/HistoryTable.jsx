import { Trash2 } from 'lucide-react';
import { formatDuration, formatWpm } from '../utils/metrics';

export default function HistoryTable({ history, onClear }) {
  if (!history.length) return <div className="panel p-10 text-center"><div className="mx-auto grid h-12 w-12 place-items-center rounded-2xl bg-white/[0.04] text-slate-500">⌁</div><h3 className="mt-4 text-lg font-semibold">No races yet</h3><p className="mx-auto mt-1 max-w-sm text-sm text-slate-500">Finish your first CodeRush race and your local progress will appear here.</p></div>;
  return <div className="panel overflow-hidden"><div className="flex items-center justify-between border-b border-white/[0.06] p-4"><div><h3 className="font-semibold">Recent races</h3><p className="text-xs text-slate-600">Stored only in this browser.</p></div><button onClick={onClear} className="inline-flex items-center gap-2 rounded-lg px-3 py-2 text-xs font-semibold text-rose-300 hover:bg-rose-400/10" aria-label="Clear race history"><Trash2 size={15}/> Clear history</button></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-white/[0.02] text-xs uppercase tracking-wider text-slate-600"><tr>{['Date','Language','WPM','Accuracy','Errors','Duration','Difficulty'].map(h=><th key={h} className="px-4 py-3 font-medium">{h}</th>)}</tr></thead><tbody className="divide-y divide-white/[0.05]">{history.map(r=><tr key={r.id} className="hover:bg-white/[0.02]"><td className="px-4 py-3 text-slate-500">{new Date(r.date).toLocaleString()}</td><td className="px-4 py-3 font-medium">{r.languageName || r.language}</td><td className="px-4 py-3 font-mono text-cyan-300">{formatWpm(r.wpm)}</td><td className="px-4 py-3">{Number(r.accuracy).toFixed(1)}%</td><td className="px-4 py-3">{r.errors}</td><td className="px-4 py-3 font-mono">{formatDuration(r.durationMs)}</td><td className="px-4 py-3 capitalize text-slate-400">{r.difficulty}</td></tr>)}</tbody></table></div></div>;
}
