import { AlertTriangle, X } from 'lucide-react';
export default function ConfirmationModal({ open, title, message, onConfirm, onCancel }) {
  if (!open) return null;
  return <div className="fixed inset-0 z-50 grid place-items-center bg-black/70 p-4 backdrop-blur-sm" role="dialog" aria-modal="true" aria-labelledby="confirm-title"><div className="panel w-full max-w-md p-6"><div className="flex items-start justify-between"><div className="grid h-11 w-11 place-items-center rounded-xl bg-rose-400/10 text-rose-300"><AlertTriangle size={20}/></div><button onClick={onCancel} className="rounded-lg p-2 text-slate-500 hover:bg-white/[0.05] hover:text-white" aria-label="Close"><X size={18}/></button></div><h2 id="confirm-title" className="mt-5 text-lg font-semibold">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{message}</p><div className="mt-6 flex gap-2"><button onClick={onCancel} className="btn-secondary flex-1">Cancel</button><button onClick={onConfirm} className="inline-flex flex-1 items-center justify-center rounded-xl bg-rose-500 px-4 py-3 font-semibold text-white hover:bg-rose-400">Clear history</button></div></div></div>;
}
